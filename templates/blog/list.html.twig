{# templates/lucky/number.html.twig #}
<ul id="navigation">
            {% for item in list %}
                <li><a href="/blog/{{item.slug }}" > {{ item.description }} </a> </li>
            {% endfor %}
        </ul>
